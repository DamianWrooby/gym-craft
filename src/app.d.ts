declare namespace App {
    interface Locals {
        user: {
            name: string;
            role: string;
            generatedPlans: number;
        };
    }
    // interface PageData {}
    // interface Error {}
    // interface Platform {}
}
