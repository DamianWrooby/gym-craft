declare namespace App {
    interface Locals {
        user: {
            id: string;
            name: string;
            role: string;
            generatedPlans: number;
        };
    }
    // interface PageData {}
    // interface Error {}
    // interface Platform {}
}
