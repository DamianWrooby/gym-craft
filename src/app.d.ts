declare namespace App {
    interface Locals {
        user: {
            name: string;
            role: string;
        };
    }
    // interface PageData {}
    // interface Error {}
    // interface Platform {}
}
